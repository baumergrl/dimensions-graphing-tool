'''
Hopper Transformation - A function that takes in a string and returns a working function that allows for input/output consistent with the mathematical transformations intended by the string.

'''

def hopperTransform(rawinput, variables):
    print("inputs to HT: ", rawinput, variables)
    steps = []
    for i in rawinput:
        if i == '(':
            marker = 0
        elif i == '+':
            pass
        elif i == 0:
            pass
        else:
            pass

def hopperQK(qk, variables):
    pass

def hopper(x, var):
    ##input:    x   =   the function to perform
    ##          var =   a list of variables to use in the function
    if x == "+":
        total = var[0] + var[1]
        return(total)
    else:
        return(0)
        
